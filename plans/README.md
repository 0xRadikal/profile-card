# Implementation Plans

Generated by the **improve** skill (read-only advisor pass) on 2026-07-13,
planned at commit `aa2739e` on branch `fix/debug-pass`.

This pass audited the whole repo — with the deepest line-by-line reading on
`index.js` and `profile-card.css` as requested. The five runtime/security
bugs from the earlier direct-fix pass are already fixed and are NOT re-listed
here. These plans cover what that pass did **not** address: verification
baseline, single-source-of-truth for content, a real CSP, and a documentation
mismatch.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Add a headless smoke test for the CLI runtime contract | P1 | M | — | DONE |
| 002  | De-duplicate blog/skills/timeline data into one JSON source | P2 | L | 001 | DONE |
| 003  | Ship a real Content-Security-Policy (remove inline scripts) | P2 | L | 001 | DONE |
| 004  | Fix README claim that pages have "no inline scripts" | P3 | S | — | DONE |

## Dependency notes

- 002 and 003 both benefit from 001 existing first: the smoke test is the
  regression net that proves the refactor/CSP did not break the runtime
  contract (every `#id` and section the JS queries).

## Findings considered and rejected

- **History ArrowUp/ArrowDown off-by-one**: traced with a 3-entry history;
  navigation is correct (idx boundaries hold, ArrowDown lands on '' at the
  end). Not a bug. (So nobody re-audits it.)
- **`cat <injection>` XSS via arg**: `files.get(arg)` returns undefined for
  any unknown key, so the `printHTML` branch is only reached for the fixed,
  trusted key set; unknown args fall to the `printText` (textContent) branch.
  Not exploitable.
- **`contactForm = qs('#contact form.card-form')` binds nothing**: verified a
  `<form class="card-form">` exists inside `#contact` (index.html:359). Binds
  correctly. Not a bug.
- **All `printHTML` callers**: every dynamic value comes from the static
  in-file data arrays (blogs/skillset/socials/milestones/achievements) or
  library-controlled strings — no untrusted input reaches innerHTML after the
  fetch fix. No further XSS surface in `index.js`.
